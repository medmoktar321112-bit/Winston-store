    workflows:
      android-release:
        name: بناء متجر وينستون
        instance_type: mac_mini_m1
        max_build_duration: 60
        scripts:
          - name: بناء ملف APK
            script: |
              cd $CM_BUILD_DIR
              ./gradlew assembleDebug
        artifacts:
          - build/app/outputs/apk/debug/app-debug.apk
